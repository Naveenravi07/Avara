

1. Use the id parameter to create a room on server. 
2. Each users joining the same meet with same id will join the room.
3. Store the user details from session while joining room. If not found then kick him .

Hashmap Rooms{
    room1 : {
        users: new Hashmap,
        transports: new Hashmap,
        consumers: new Hashmap,,
        producers: new Hashmap, 
    },
    room2 : {
        users: new Hashmap,
        transports: new Hashmap,
        consumers: new Hashmap,,
        producers: new Hashmap, 
    },
}
}

